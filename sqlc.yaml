version: "1"
project:
    id: "buss"
packages:
    - path: "./db/sqlc"
      name: "db"
      engine: "postgresql"
      schema: "./db/migration"
      queries: "./db/query"
      emit_json_tags: true
      emit_exact_table_names: false